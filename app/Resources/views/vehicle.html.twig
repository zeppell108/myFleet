{% extends "base.html.twig" %}

{% block title %}VEHICLE{% endblock %}

{% block link %}
    <link rel="stylesheet" href="{{ asset('bundles/app/css/vehicle.css') }}" type="text/css" media="all">
{% endblock %}

{#{%  block header %}#}
    {#<div class="panel panel-primary visible-xs">#}
        {#<div class="panel-heading">Logowanie</div>#}
        {#<div class="panel-body">#}
            {#{% block loginForm %}#}
                {#{{ parent() }}#}
            {#{% endblock loginForm %}#}
        {#</div>#}
    {#</div>#}
{#{%  endblock header %}#}

{% block body %}

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit">
    {{ form_end(form) }}

{% endblock %}

{% block javascripts %}
    {{ parent() }}
    <script>
        renderFormEventPart();
        var newElement = $("<div class='new'> This is new element</div>");
        var foo = $("#formColor");

        newElement.appendTo(foo);
        alert('function');
    </script>
{% endblock %}